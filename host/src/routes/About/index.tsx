import React from "react";
import Layout from "../../containers/Layout";

const About = () => {
  return (
    <Layout>
      <div>
        <p>About Host</p>
      </div>
    </Layout>
  );
};

export default React.memo(About);
