import React from "react";
import Layout from "../../containers/Layout";

const Contact = () => {
  return (
    <Layout>
      <div>
        <p>Contact Host</p>
      </div>
    </Layout>
  );
};

export default React.memo(Contact);
